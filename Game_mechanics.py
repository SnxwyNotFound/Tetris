# Game mechanics
import blocks


def rotate_clockwise(shape):
    rows = len(shape) -1
    columns = len(shape[0]) -1
    for item_row in shape:



# def place_block(shape):


def spawn_item(item):
    if item == blocks.I:
        spawn("row 22", "column 5,6")
rotate_clockwise()